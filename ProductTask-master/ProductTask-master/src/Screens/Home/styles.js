import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 8,
  },
  searchContainer: {
    marginBottom: 8,
  },
  scrollView: {
    marginBottom: 60,
  },
  productContainer: {},
});
